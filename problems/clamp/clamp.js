
function clamp(value, lowerBound, upperBound) { 
  // Add any parameters you need. Good luck!
  // return lowerbound if value is less 
  if (value > upperBound) {
    return upperBound;
  } else if (value < lowerBound) {
    return lowerBound;
  } else {
    return value;
  };
  
}
  // return upperbound if value is more 
  // function returns value if it is between bounds


