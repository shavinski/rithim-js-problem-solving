
function createMatrix(numCols, numRows) { 
  // Add any parameters you need. Good luck!
  const result = [];

  for (let i = 0; i < numRows; i++) {
    result.push([0]);
  };

  console.log(result);
  return result;

}
